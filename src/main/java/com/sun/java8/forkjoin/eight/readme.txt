1.ForkJoinPool主要用于实现“分而治之”算法，特别是分治之后递归调用的函数。
2.适用计算密集型的任务，如果存在 I/O，线程间同步，sleep() 等会造成线程长时间阻塞的情况时，最好配合使用 ManagedBlocker
3.伪共享：缓存系统中是以"缓存行"为单位来存储的。缓存行是2的整数幂个连续字节，一般为32~256，常用64个字节。
当多线程修改变量时，如果变量在同一个缓存行，就会无意中影响彼此的性能，这就是伪共享。
JDK8适用@sun.misc.Contended来避免伪共享，原理是在使用此注解的对象或者字段前后各增加128字节大小的padding.